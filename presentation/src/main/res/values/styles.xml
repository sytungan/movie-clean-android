<resources>

    <style name="BaseAppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="materialButtonStyle">@style/MovieDB.Button</item>
    </style>

    <style name="AppTheme" parent="BaseAppTheme" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="AppTheme.ItemSelectable" />

    <style name="InformationRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/selector_rating_bar_information</item>
        <item name="android:maxHeight">14dp</item>
        <item name="android:minHeight">14dp</item>
    </style>

    <style name="YourRateRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/selector_rating_bar_your_rate</item>
        <item name="android:maxHeight">30dp</item>
        <item name="android:minHeight">30dp</item>
    </style>

    <style name="CommentsRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/selector_rating_bar_comments</item>
        <item name="android:maxHeight">9dp</item>
        <item name="android:minHeight">9dp</item>
    </style>

    <style name="MovieDB.Button" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:padding">0dp</item>
        <item name="backgroundTint">@color/color_bg_button</item>
        <item name="elevation">0dp</item>
    </style>

</resources>
